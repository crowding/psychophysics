function result=initeyelink(dummy)% USAGE: result=initeyelink([dummy])% dummy: omit, or set to 0 to attempt initialization,% set to 1 to use initializedummy% if regular initialization fails, it will provide option% for dummy initilization% returns 1 when succesful, 0 otherwise% history% 28-10-02	fwc	extracted it from other program%result=0;if ~exist('dummy', 'var') | isempty(dummy)	dummy=0;endwindow=SCREEN('GetFrontWindow');if dummy==1	eyelink('initializedummy');else	if eyelink('initialize') ~= 0		yesno=SCREEN(window,'Dialog','Run in dummymode?', 'Yes' );		if strcmp( lower(yesno), 'yes') | strcmp( lower(yesno), 'ja')			eyelink('initializedummy');		else			return;		end	endendresult=1;